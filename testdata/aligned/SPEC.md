# Key-Value Store Spec

## Operations

- The store must support Get(key) returning a value.
- The store must support Set(key, value) to store data.
- The store must support Delete(key) to remove data.
